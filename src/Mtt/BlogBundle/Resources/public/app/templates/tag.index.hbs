<div id="flashes">
</div>

<div class="page-header">
    <div class="actions-menu">
        <a class="btn btn-primary" href="#"
            {{action "openModal"}}>
            <i class="glyphicon glyphicon-plus-sign"></i>
            <span>Создать тег</span>
        </a>
    </div>
    <h1><i class="glyphicon glyphicon-tag"></i> Управление тегами.</h1>
</div>

{{paginator-list meta=model.meta routeName='tag.index'}}

<table class="table table-bordered">
    <thead>
    <tr>
        <th class="center-text">#id</th>
        <th>Тег</th>
        <th>slug</th>
        <th class="text-right">Количество записей</th>
        <th>&nbsp;</th>
    </tr>
    </thead>
    <tbody>
    {{#each tag in sortableTags}}
        <tr>
            <td class="center-text">{{tag.id}}</td>
            <td>{{tag.name}}</td>
            <td>{{tag.url}}</td>
            <td class="text-right">-</td>
            <td>
                <div class="pull-right">
                    <a class="btn btn-primary" href="#">
                        <i class="glyphicon glyphicon-pencil"></i>
                        <span>edit</span>
                    </a>
                    <button class="btn btn-danger btn-confirm">
                        <i class="glyphicon glyphicon-trash"></i>
                        <span>delete</span>
                    </button>
                </div>
            </td>
        </tr>
    {{/each}}
    </tbody>
</table>

{{paginator-list meta=model.meta routeName='tag.index'}}

{{partial 'footer'}}

<div class="modal fade" id="modal_new_tag" tabindex="-1" role="dialog"
     aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button"
                        class="close"
                        aria-label="Close"
                    {{action "closeModal"}}>
                    <span aria-hidden="true">&times;</span>
                </button>
                <h4 class="modal-title">Новый тег</h4>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label for="tag_field" class="control-label">Тег:</label>
                        <input id="tag_field" class="form-control" type="text"/>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                {{#if enableNewTagButton}}
                    <button type="button"
                            class="btn btn-primary"
                        {{action "createTag"}}>Создать</button>
                {{else}}
                    <button type="button"
                            class="btn btn-primary"
                            disabled="disabled">Создать</button>
                {{/if}}
            </div>
        </div>
    </div>
</div>
